<%= render FlatPack::PageTitle::Component.new(
  title: @folder.name,
  subtitle: "Folder demo"
) %>

<div class="mt-4 space-y-2">
  <%= link_to "Move (full page)", recording_studio_moveable.move_recording_path(recording_id: @recording.id), class: "underline" %>
  <br>
  <%= link_to "Move (modal mode)", recording_studio_moveable.move_recording_path(recording_id: @recording.id, display: "modal"), class: "underline" %>
</div>
