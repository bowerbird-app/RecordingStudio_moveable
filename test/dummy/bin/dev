#!/usr/bin/env sh

if ! gem list foreman -i --silent; then
  echo "Installing foreman..."
  gem install foreman
fi

# Default to port 3000 if not specified
export PORT="${PORT:-3000}"

# Let the debug gem allow remote connections,
# but avoid loading until `debugger` is called
export RUBY_DEBUG_OPEN="true"
export RUBY_DEBUG_LAZY="true"

echo "Building Tailwind CSS before starting the development processes..."
bin/rails tailwindcss:build || exit 1

echo "Preparing database and seeding demo data before starting the development processes..."
bin/rails db:prepare db:seed || exit 1

exec foreman start -f Procfile.dev "$@"
